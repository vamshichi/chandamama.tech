/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['your-external-image-domain.com'], // Replace with the actual domain(s)
    },
  }
  
  export default nextConfig;
  